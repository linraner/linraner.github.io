language: node_js  
node_js: stable     
cache:
    directories:
        - node_modules    
before_install:
  - npm install -g hexo
install:
  - npm install   
before_script:
  - npm install -g mocha
  - git clone --branch master https://github.com/linraner/linraner.github.io.git
script:
  - hexo cl   
  - hexo g   

after_script:
  - cd ./public
  - git init
  - git config user.name "linran"   #修改成自己的github用户名
  - git config user.email "linran@live.com"   #修改成自己的GitHub邮箱
  - git add .
  - git commit -m "update by Travis-CI"
  - git push --force --quiet "https://${GITHUB_TOKEN}@${GH_REF}" master:master 

branches:
  only:
  - doc
env:
    global:
            - GH_REF: https://github.com/linraner/linraner.github.io.git
